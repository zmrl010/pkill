[package]
name = "pkill"
description = "Simple tool to kill processes"
version = "0.1.0"
edition = "2021"
authors = ["zmrl"]
license = "MIT"

[dependencies]
sysinfo = "0.26"
clap = { version = "4.0", features = ["derive"] }
anyhow = "1.0"

[build-dependencies]
clap = { version = "4.0", features = ["derive"] }
clap_mangen = "0.2.3"
sysinfo = "0.26"
