[package]
name = "pkill"
description = "Simple tool to kill processes"
version = "0.1.0"
edition = "2021"
authors = ["zmrl"]
license = "MIT"

[dependencies]
sysinfo = "0.26"
clap = { version = "4.0", features = ["derive"] }
anyhow = "1.0"